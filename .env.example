# Twilio SMS credentials required by convex/auth.ts -> requestOtp
#
# IMPORTANT: These must be configured in your Convex deployment environment
# (not in your local Next.js .env). This file is only a reference/template.
#
# Where to get these values:
# - TWILIO_ACCOUNT_SID: Twilio Console -> Account -> General Settings
#   (Starts with "AC" and is 34 characters long)
# - TWILIO_AUTH_TOKEN: Twilio Console -> Account -> General Settings (Auth Token)
# - TWILIO_FROM_NUMBER: A Twilio phone number you own, in E.164 format (e.g. +15551234567)
#
# How to set in Convex (CLI):
#   npx convex env set TWILIO_ACCOUNT_SID "ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#   npx convex env set TWILIO_AUTH_TOKEN "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#   npx convex env set TWILIO_FROM_NUMBER "+15551234567"
# Verify:
#   npx convex env list
#
# How to set in Convex (Dashboard):
#   Open the Convex Dashboard for your project -> Environment Variables -> Add variables below.
#
# Security:
# - Never commit real credentials to version control.
# - Treat the Auth Token like a password. Rotate if leaked.

TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM_NUMBER=+15551234567
